/target
**/*.rs.bk
Cargo.lock.orig
# local runtime handshake (port+token) written by the daemon
runtime.json
*.db
*.db-shm
*.db-wal
.DS_Store

# internal release notes — not published
/RELEASING.md

# Claude Code's per-session tooling cache — local scratch, never part of the
# repo or a release (it leaked into v0.2.1's source.tar.gz).
.claude/
